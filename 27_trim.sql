-- list all the last name of all customers and remove all the white spaces in the names
-- Trim() - remove leading and trailing white spaces from a string
-- LTrim() - Left space trim
-- RTrim() -eight space trim
SELECT trim(last_name)
FROM customers